# خطوات تفعيل قاعدة البيانات (Google Firebase) مجاناً 🔥

لجعل التطبيق يعمل "أونلاين" ويحفظ البيانات بشكل حقيقي، اتبع هذه الخطوات البسيطة (مرة واحدة فقط).

## 1. إنشاء المشروع
1.  ادخل على موقع: [console.firebase.google.com](https://console.firebase.google.com/)
2.  سجل الدخول بحساب الجيميل الخاص بك.
3.  اضغط **Create a project**.
4.  سمّهِ مثلاً: `RawanDietApp`.
5.  عطل خيار "Google Analytics" (غير مهم الآن) -> اضغط **Create Project**.

## 2. إنشاء قاعدة البيانات (Firestore)
1.  من القائمة الجانبية اليسرى، اختر **Build** ثم **Firestore Database**.
2.  اضغط **Create database**.
3.  اختر الخيار **Start in test mode** (للبساطة حالياً).
4.  اضغط **Next** ثم اختر السيرفر (europe-west مثالي) ثم **Enable**.

## 3. الحصول على المفاتيح (API Keys)
1.  اضغط على أيقونة **الإعدادات** (الترس ⚙️) بجانب Project Overview في الأعلى اليسار.
2.  اختر **Project settings**.
3.  انزل لأسفل الصفحة لقسم **Your apps**.
4.  اضغط على أيقونة الويب `</>`.
5.  اكتب اسم للتطبيق (مثلاً `Web`) واضغط **Register app**.
6.  سيظهر لك كود "firebaseConfig". **انسخ هذا الكود**.

## 4. وضع الكود في مشروعك
1.  افتح ملف `js/firebaseConfig.js` في مجلد مشروعك.
2.  امسح الكود القديم والصق الكود الذي نسخته من جوجل.
3.  احفظ الملف.

**مبروك!** 🎉 
الآن، أي مشترك تضيفه من اللابتوب سيظهر فوراً في الموبايل، والبيانات محفوظة في سيرفرات جوجل بأمان.
